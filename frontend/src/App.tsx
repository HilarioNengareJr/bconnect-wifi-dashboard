export default function App() {
  return (
    <div className="min-h-screen bg-background text-text-primary">
      <header className="px-6 h-16 flex items-center">
        <span className="text-lg font-bold">b connect</span>
      </header>
      <main className="mx-auto max-w-[1280px] p-8">
        <h1 className="text-2xl font-semibold">Wi-Fi Controller Dashboard</h1>
        <p className="mt-2 text-text-secondary">
          Project shell. Sync status, venues, and sessions arrive in later features.
        </p>
      </main>
    </div>
  );
}
